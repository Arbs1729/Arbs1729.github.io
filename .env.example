# Astro bakes this public Worker URL into the site at build time.
PUBLIC_CHAT_ENDPOINT=https://aryan-portfolio-copilot.f20202091.workers.dev/chat

# Use this instead when running the local Worker adapter.
# PUBLIC_CHAT_ENDPOINT=http://127.0.0.1:8787/chat
